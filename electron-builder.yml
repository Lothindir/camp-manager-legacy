appId: "com.electron.camp-manager"
productName: "Camp Manager"
win:
  target:
    - "nsis"
    - "portable"
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  artifactName: "${productName} Setup beta-${version}.${ext}"
portable:
  artifactName: "${productName} Portable beta-${version}.${ext}"